// import type { NextConfig } from "next";
// /** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  output: 'export',
  typescript: {
    ignoreBuildErrors: true,
  },
// };
silent: true,
org: "valentin-madiot",
project: "portfoli0-v2",
}

export default nextConfig;